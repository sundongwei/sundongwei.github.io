<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://sundongwei.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://sundongwei.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-06-13T12:59:55+00:00</updated><id>https://sundongwei.github.io/feed.xml</id><title type="html">blank</title><subtitle>Sun Dongwei&apos;s personal academic website </subtitle><entry><title type="html">Opencvconfig</title><link href="https://sundongwei.github.io/blog/2024/OpencvConfig/" rel="alternate" type="text/html" title="Opencvconfig"/><published>2024-06-13T00:00:00+00:00</published><updated>2024-06-13T00:00:00+00:00</updated><id>https://sundongwei.github.io/blog/2024/OpencvConfig</id><content type="html" xml:base="https://sundongwei.github.io/blog/2024/OpencvConfig/"><![CDATA[<ol> <li><strong>安装</strong></li> </ol> <p>⾸先先安装⼀切有必要的包裹：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```Shell
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install build-essential cmake
unzip pkg-config
sudo apt-get install libjpeg-dev libpng-dev
libtiff-dev
sudo apt-get install libavcodec-dev
libavformat-dev libswscale-dev
sudo apt-get install libv4l-dev libxvidcore-
dev libx264-dev
sudo apt-get install libgtk-3-dev
sudo apt-get install libatlas-base-dev
gfortran
sudo apt-get install python3-dev
```
</code></pre></div></div> <ol> <li><strong>下载OpenCV源代码</strong></li> </ol> <p>接下来我们下载OpenCV的源代码。这是因为如果要配置CUDA⽀持，OpenCV并没有提供现成直接安装的包裹 （C++版也是如此，不像Python的能直接pip install），我们必须先下载源代码，然后cmake编译后安装。</p> <p>1.进⼊OpenCV Github Release⽹⻚： <a href="https://github.com/opencv/opencv/releases">https://github.com/opencv/opencv/releases</a></p> <p>2.选择你想要的版本，我选择了最新的4.4.0.<br/> <img src="../assets/img/openncv_image0.png" alt="image.png"/></p> <p>3.接下来轮到OpenCV-contrib：选择⼀样版本的contrib（不⼀样版本后果自负哈） <a href="https://github.com/opencv/opencv_contrib/releases">https://github.com/opencv/opencv_contrib/releases</a></p> <ol> <li><strong>解压</strong></li> </ol> <p>在home下打开终端。加上从上⾯拿到的两个链接，通过以下命令，我们能下载OpenCV的源代码并解压:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```Shell
cd ~
wget -O opencv.zip
https://github.com/opencv/opencv/archive/[你的版本号].zip
wget -O opencv_contrib.zip
https://github.com/opencv/opencv_contrib/archive/[你的版本号].zip
unzip opencv.zip
unzip opencv_contrib.zip
mv opencv-[你的版本号] opencv
mv opencv_contrib-[你的版本号] opencv_contrib
```
</code></pre></div></div> <ol> <li> <p><strong>选择编译</strong></p> <p>使⽤cmake-gui，选择unix makefiles, 然后根据视频把不需要的项⽬取消掉，主要有:opencv_face, opencv_world，所有test，opencv_xfeature2d,opencv_stitching。记得把<strong>OPENCV_GENERATE_PKGCONFIG</strong> 这个勾选上，要不然没法查看Opencv版本，还得⾃⼰创建。</p> <p><a href="https://www.bilibili.com/video/BV1Rp4y1a7cm?spm_id_from=333.999.0.0&amp;vd_source=4fb36de1266349e65e8682fb847af8ee">https://www.bilibili.com/video/BV1Rp4y1a7cm?spm_id_from=333.999.0.0&amp;vd_source=4fb36de1266349e65e8682fb847af8ee</a></p> </li> <li> <p><strong>编译</strong></p> <pre><code class="language-Shell"> 编译make -jx,x是CPU核数，随便写⼀个也可以。例如：8
</code></pre> </li> <li> <p><strong>安装</strong></p> <pre><code class="language-Shell"> 编译成功后我们就可安装：
 sudo make install
 sudo ldconfig
</code></pre> </li> <li> <p><strong>验证</strong></p> <p>注意加 4</p> </li> </ol> <p><img src="../assets/img/openncv_image1.png" alt="image.png"/></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>· **PKG_CONFIG作用**

一般我们写的程序都是要依赖一些库，但库的安装位置可能不同，这就需要一个工具能够管理并能搜索这些库的路径（头文件路径 `/include`，库文件路径 `/lib`）。

`pkg-config` 就是通过库提供的一个 `.pc` 文件获得库的各种必要信息的，包括版本信息、编译和连接需要的参数等。通过 `pkg-config` 提供的参数`–cflags, –libs`，将所需信息提取出来供编译和连接使用。这样，不管库文件安装在哪，通过库对应的 `.pc` 文件就可以准确定位。

它提供的主要功能有:

- 检查库的版本号。如果所需库的版本不满足要求，打印出错误信息，避免连接错误版本的库文件；

- 获得编译预处理参数，如宏定义，头文件的路径；

- 获得编译参数，如库及其依赖的其他库的位置，文件名及其他一些连接参数；

- 加入所依赖的其他库的设置

**· PKG_CONFIG_PATH**

`pkg-config` 默认会搜索 `/usr/lib/pkgconfig` 和`/usr/share/pkgconfig`下的 `.pc` 配置文件，若我们源码编译的库的路径不在 `pkg-config` 的默认搜索路径下，则可以通过环境变量 `PKG_CONFIG_PATH` 将自定义的路径添加到 `pkg-config` 的搜索路径。
</code></pre></div></div> <h4 id="最终配置图">最终配置图</h4> <p><img src="../assets/img/openncv_image2.png" alt="image.png"/></p> <p><img src="../assets/img/openncv_image3.png" alt="image.png"/></p> <ol> <li> <p><strong>测试</strong></p> <ul> <li> <p>创建新⽂件夹，我命名为OpenCV-Test</p> </li> <li> <p>进⼊⽂件夹后右键打开终端</p> </li> <li> <p>touch main.cpp 创建名为main.cpp的⽂件</p> </li> <li> <p>右键main.cpp，使⽤IDE打开</p> </li> <li> <p>复制代码：</p> <pre><code class="language-C++"> #include &lt;opencv2/opencv.hpp&gt;
 #include &lt;iostream&gt;
 using namespace cv;
 using namespace std;
 int main(int argc, char** argv)
 {
 //读取照⽚
  Mat image = imread("OpenCV_Logo.png");
 //检测失误
 if (image.empty())
 {
  cout &lt;&lt; "Could not open or find the image"&lt;&lt; endl;
  cin.get(); //等待键盘输⼊
  return -1;
 }
  String windowName = "OpenCV Test";
 窗⼝名称
 namedWindow(windowName);
 创建新窗⼝
 imshow(windowName, image);
 使⽤新窗⼝显⽰照⽚
 waitKey(0);
 等待键盘输⼊
 destroyWindow(windowName);
 关闭所有窗⼝
 return 0;
 }
</code></pre> </li> </ul> </li> </ol>]]></content><author><name></name></author><summary type="html"><![CDATA[安装]]></summary></entry><entry><title type="html">ProtobufConfiguration</title><link href="https://sundongwei.github.io/blog/2024/ProtobufConfig/" rel="alternate" type="text/html" title="ProtobufConfiguration"/><published>2024-05-14T11:26:00+00:00</published><updated>2024-05-14T11:26:00+00:00</updated><id>https://sundongwei.github.io/blog/2024/ProtobufConfig</id><content type="html" xml:base="https://sundongwei.github.io/blog/2024/ProtobufConfig/"><![CDATA[<p>1.<strong>依赖</strong></p> <pre><code class="language-Shell">sudo apt-get install autoconf automake libtool
curl make g++ unzip
</code></pre> <pre><code class="language-Shell">git clone
https://github.com/protocolbuffers/protobuf.git
cd protobuf
git submodule update --init --recursive
./autogen.sh
</code></pre> <p>2.<strong>to build and install the C++ Protocol Buffer runtime and the Protocol Buffer compiler (protoc) execute the following:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```Shell
./configure --prefix=指定路径
make
make check
sudo make install
sudo ldconfig # refresh shared library cache.

到此步还没有安装完毕，
####### add protobuf lib path ######

#（动态库搜索路径）程序加载运行期间查找动态链接库时指定除了系统默认路径之外的其他路径
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/protobuf/lib/ 

#（静态库搜索路径）程序编译期间查找动态链接库时指定查找共享库的路径
export LIBRARY_PATH=$LIBRARY_PATH:/usr/local/protobuf/lib/

#执行程序搜索路径
export PATH=$PATH:/usr/local/protobuf/bin/ 

#c程序头文件搜索路径
export C_INCLUDE_PATH=$C_INCLUDE_PATH:/usr/local/protobuf/include/ 

#C++程序头文件搜索路径
export CPLUS_INCLUDE_PATH=$CPLUS_INCLUDE _PATH:/usr/local/protobuf/include/ 

#pkg— config 路径
export PKG_CONFIG PATH=/usr/local/protobuf/lib/pkgconfig/
###############################################
```
</code></pre></div></div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/pro2buf1-480.webp 480w,/assets/img/pro2buf1-800.webp 800w,/assets/img/pro2buf1-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/pro2buf1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/pro2buf2-480.webp 480w,/assets/img/pro2buf2-800.webp 800w,/assets/img/pro2buf2-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/pro2buf2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>3.<strong>编译Python</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>```Shell
cd python
python setup.py build 
python setup.py test
python setup.py install
```

 验证

```Shell
python
import google.protobuf
```

卸载

```Shell
sudo rm /usr/local/bin/protoc //执⾏⽂件
sudo rm -rf /usr/local/include/google //头⽂件
sudo rm -rf /usr/local/lib/libproto* //库⽂件
```
</code></pre></div></div>]]></content><author><name></name></author><category term="Tools_Config"/><category term="note"/><summary type="html"><![CDATA[a tools config]]></summary></entry></feed>